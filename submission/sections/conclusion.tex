% =============================================================================
% CONCLUSION   --   target ~0.25 pp (~4-5 sentences, no new content)
% Beats: (1) we ran the first fairness audit of cervical-spine MRI seg
% (sex/race/age, CSpineSeg). (2) Model fair by every measure. (3) Headline:
% reference-label provenance is a first-order confounder -- swapping the ruler
% flips the age verdict via variance collapse ("false confidence", complementary
% to Parikh et al.'s "false magnitude"); ~8-pt raw inflation affects any user of a
% mixed-label dataset. (4) Limits: single
% institution/anatomy/architecture + high-performance ceiling; generalisation open.
% =============================================================================
\section{Conclusion}

We presented the first demographic fairness audit of cervical-spine MRI segmentation:
across sex, race, and age the deployed model is fair by every standard metric. The
decisive finding is methodological. Because a mixed-provenance dataset's silver labels
are generated by a model trained on its gold labels, any new model trained on those
same gold labels resembles the silver labels more than it resembles expert truth -- so
scoring against silver overestimates performance by ${\sim}8$ Dice points and flips the
age verdict from non-significant to significant by collapsing within-group variance
(\emph{false confidence}), the complement to Parikh et al.'s \emph{false magnitude}.
Reference-label provenance is thus a first-order confounder in segmentation
evaluation: on any mixed-label dataset, performance and fairness should be reported
against expert labels. Our evidence is a single institution, anatomy, and architecture in
a high-performance regime; whether the same leakage distorts harder tasks remains open.

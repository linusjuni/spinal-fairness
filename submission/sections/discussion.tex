% =============================================================================
% DISCUSSION   --   target ~0.75 pp (COMPRESS hard)
% No subsections -- all beats are \paragraph{} run-ins. Limitations +
% recommendations are compact INLINE lists (cheaper than enumerate). Points
% drawn from paper/sections/discussion.tex. E3/E4 amplification thread cut for 8pp
% (least-supporting of Aasa's leakage story); M_silver kept only as leakage evidence.
% =============================================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{Two modes of ruler bias.}
Our silver ruler is $M_{\mathrm{gold}}$'s predictions, so scoring against it measures
inter-model \emph{agreement}, not accuracy. That agreement (${\sim}0.97$ Dice, with
the within-group SD collapsing ${\sim}4\times$ from $0.058$ to $0.014$) far exceeds
the ${\sim}0.89$ the same predictions reach against expert labels, and the cause is
label leakage: $M_{\mathrm{mix}}$ and the $M_{\mathrm{gold}}$ ruler are both trained,
in part, on the same expert labels, so two models fitted to overlapping data resemble
each other more than either resembles the truth. The leakage is intrinsic to the
dataset's labels: its silver labels are themselves generated by a model trained on its
gold labels, so expert-annotation conventions propagate into any model that learns from
them. Because the Kruskal--Wallis
test's power scales with signal-to-noise, the same clinically negligible age gradient
that gold's noise buries clears FDR correction once the leakage-driven variance
collapse sharpens it: the ruler changed the test's power, not the patient. This
complements Parikh et al.~\cite{parikh2025labelbias}, whose \emph{independently
biased} silver ruler inflates the between-group gap (DIR $0.871 \to 0.815$) --
\emph{false magnitude}; our \emph{correlated} ruler instead shrinks the gap yet
manufactures significance by removing noise -- \emph{false confidence}. Same
practical hazard (the verdict depends on the ruler), opposite mechanism.

\paragraph{The age effect is intrinsic, not unfairness.}
The silver-ruler significance is an artefact, but the age \emph{trend} is real: it
runs the same direction on both rulers (60+ worst) and matches the degenerative
anatomy of ``\nameref{sec:confounders}'' -- intrinsic difficulty, not a label
artefact, and not unfairness either, the magnitudes being clinically negligible
(${\le}2.7$ Dice points, every group $>0.89$, all gold-ruler DIRs $>0.93$).

\paragraph{Limitations.}
(i)~a high-performance ceiling (all groups $>0.89$ Dice) leaves little room for
disparity to emerge; (ii)~a single institution, anatomy, sequence, and architecture,
with no external validation, and a small gold test set ($76$, ${\sim}20$ Black) that
limits power for race; and (iii)~a correlated silver ruler -- a near-clone of the
model under test -- so we can demonstrate only false confidence, not false magnitude.

\paragraph{Recommendations.}
(1)~\textbf{Report against expert labels.} On a mixed-provenance dataset, machine
labels leak information from the expert labels they were derived from, so validating
on them overestimates performance (${\sim}8$ Dice points here); the only safe
reference is the expert subset, which presupposes users are told which labels are
which. (2)~\textbf{State where reference labels come from}: swapping expert for machine
labels moved our verdict from $0/63$ to $11/63$ significant on identical predictions.
(3)~\textbf{Check ruler--model similarity}: scores much higher \emph{and} much tighter
against the ruler than against experts (${\sim}0.97$ vs.\ ${\sim}0.89$, $4\times$
variance collapse) signal a ruler too close to be an independent benchmark.
(4)~\textbf{Report magnitude, not just significance}: the flagged age gap spans $0.6$
Dice points where every group exceeds $0.97$. Eliciting false magnitude with an
independent ruler, and a formal variance-collapse diagnostic, remain for future work.
